import "./QuestionModal.css";

const QuestionModal = ({ closeModal2 }) => {
  return (
    <div class="container-questionModal">
      QuestionModal
      <button onClick={closeModal2}>close</button>
    </div>
  );
};

export default QuestionModal;
